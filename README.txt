# BUILD-USING:        docker build -t nesrait/btsync .
# RUN-USING one of:
#                     docker run --name DATA -v /data nesrait/btsync
#                     docker run --name DATA -v <data_folder> nesrait/btsync <data_folder>
#                     docker run --name DATA -v <data_folder> nesrait/btsync <data_folder> <btsync_folder_secret>
